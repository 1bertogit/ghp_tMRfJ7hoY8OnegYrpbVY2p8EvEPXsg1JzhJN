'use client';

import { cn } from '@/lib/utils';

// This component is currently disabled to improve performance.
export function FloatingOrb({ className, delay = 0 }: { className?: string; delay?: number; }) {
  return null;
}
